import React from 'react'

const ColoredLine = ({ color }) => (
    <hr
        style={{
            color: 'black',
            backgroundColor: 'black',
            height: 3

        }}
    />
);


export default ColoredLine